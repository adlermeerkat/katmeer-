- provide(:title, @user.username)

%h1
	= @user.username
%p
	Email: #{@user.email} #{ link_to 'edit', edit_user_path(@user, 'email')}
%p
	Password: *hidden* #{ link_to 'edit', edit_user_path(@user, 'password')}
